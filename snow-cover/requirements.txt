geopandas>=0.13.0
shapely>=2.0.0
pyproj>=3.4.0
numpy>=1.21.0

# VIIRS data download dependencies
requests>=2.28.0
h5py>=3.7.0

# Postgres cache (src/postgres_cache.py)
asyncpg>=0.29.0

# GDAL Python bindings, used by scripts/hdf-to-geotiff.py via `from osgeo import gdal`.
# Not installable standalone from PyPI in the general case: the wheel must match the
# libgdal installed on the system, so install GDAL first and then a matching version.
# gdal>=3.6.0

# Testing dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
